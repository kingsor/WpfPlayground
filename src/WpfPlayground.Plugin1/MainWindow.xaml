<Window x:Class="WpfPlayground.Plugin1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:WpfPlayground.Plugin1.Converters"
        xmlns:local="clr-namespace:WpfPlayground.Plugin1"
        mc:Ignorable="d"
        Title="{Binding CustomTitle}"
        Height="450"
        Width="800">
    <Window.Resources>
        <converters:NicksCoolConverter x:Key="NicksCoolConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Grid.Row="0"
               Content="{Binding CoolLevel, Converter={StaticResource NicksCoolConverter}}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center" />
        <TextBox Grid.Row="1"
                 Text="{Binding FancyText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Width="400"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center" />
    </Grid>
</Window>
